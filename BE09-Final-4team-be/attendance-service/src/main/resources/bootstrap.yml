spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: attendance-service
      profile: default
      fail-fast: true
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 6
      label: main
